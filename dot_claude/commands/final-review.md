---
description: Run final validation and review for the full task
---

Read `.agent-task`, resolve `$HOME/agent-vault/<contents-of-.agent-task>`, then
use `task.md`, optional `research.md`, and `plan.md` as source of truth. Run the
smallest final validation, review the full diff against base, and write concise,
actionable feedback to `review.md`; preserve structure; no history or repeated
context.

Return validation result, review verdict, blocking findings, merge readiness,
and deferred follow-up work.
