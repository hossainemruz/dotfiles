---
description: Refine the selected Task requirements
---

Run `taskctl context` and read `artifacts.task`. Check goal, acceptance criteria,
constraints, non-goals, conflicts, edge cases, and validation expectations
without changing scope. If clear, make no edits. Otherwise propose focused edits
or questions and get confirmation before modifying `task.md`; preserve its
structure and prose.

Return Task ID/title/path, verdict, blockers, and `/research` or `/plan`.
