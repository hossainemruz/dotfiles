# Markdown Formatting

When creating or editing Markdown, do not hard-wrap prose. Keep each newly authored or modified paragraph and list item on one physical line. Add line breaks only when required by Markdown structure, such as headings, separate list items, blockquotes, tables, and fenced code, or when preserving user-authored formatting. Do not reflow untouched content solely to change its wrapping.
