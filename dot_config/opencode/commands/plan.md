---
description: Research and plan the complete selected Task as reviewable Subtasks.
agent: orchestrator
subtask: false
---

Execute the canonical planning workflow through the temporary bridge. Treat `$ARGUMENTS` as optional constraints or emphasis that cannot override `task.md`. Resolve Task context, clarify blocking requirements yourself, obtain bounded repository evidence from `@explore`, dispatch stateless `@planner` with the complete packet, persist its research and ordered multi-Subtask plan, and apply the matching one-PR/one-Step taskctl compatibility hierarchy. Do not edit repository source.

Return Subtask count and order, Advisor decisions needed, blockers, branch preparation, and `/next-subtask` when work is eligible.
