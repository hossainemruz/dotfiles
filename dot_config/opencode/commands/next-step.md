---
description: Select or continue the next planned Step, implement it, and submit it for acceptance.
agent: orchestrator
subtask: false
---

Execute the canonical planned-Step selection and workstream protocol with `@builder`. `$ARGUMENTS` is bounded feedback or implementation guidance for the selected Step. This command authorizes required PR/Step starts and submission after builder readiness, but never Step acceptance or completion; route a corrective Step to `/address-review`.
