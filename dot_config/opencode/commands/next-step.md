---
description: Select or continue the next planned Step, implement it, and submit it for acceptance.
agent: orchestrator
subtask: false
---

Execute the canonical planned-Step selection and workstream protocol, selecting `@builder` or `@builder-high` through the orchestrator's routing rules. `$ARGUMENTS` is bounded feedback or implementation guidance for the selected Step and may explicitly request high effort. This command authorizes required PR/Step starts and submission after builder readiness, but never Step acceptance or completion; route a corrective Step to `/address-review`.
