---
description: Select the next PR and implement a high-risk Step with extended reasoning.
agent: orchestrator
subtask: false
---

Execute the canonical planned-Step selection and workstream protocol, but force `@builder-high` for the selected Step. `$ARGUMENTS` remains bounded Step feedback or guidance. This command authorizes required PR/Step starts and submission after builder readiness, but never acceptance or completion; route a corrective Step to `/address-review`.
