---
description: Refine the selected Task requirements.
---

Run `taskctl context` and read `artifacts.task`. Check goal, acceptance criteria, constraints, non-goals, conflicts, edge cases, and validation expectations without changing scope. If clear, make no edits. Otherwise, ask question one by one until all ambiguity and unknown are resolved. Propose your recommendation along with other options. Once everything clear, update `task.md`; preserve its structure and prose.

Return Task ID/title/path, verdict, blockers, and `/research` or `/plan`.
