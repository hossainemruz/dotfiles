---
description: Implement, validate, and automatically review the next eligible Subtask.
agent: orchestrator
subtask: false
---

Execute the canonical Subtask workflow through the temporary bridge. `$ARGUMENTS` is bounded implementation guidance that cannot widen the frozen contract. Select and start only the first eligible Subtask, reconcile its context through `@explore`, obtain any required `@advisor` directive, dispatch `@builder`, validate through `@executor`, and run the standard or risk-triggered expert review and bounded remediation loop. Stop at logical `ready_for_human_review`, `blocked`, or another explicit blocker; never accept or complete the Subtask.
